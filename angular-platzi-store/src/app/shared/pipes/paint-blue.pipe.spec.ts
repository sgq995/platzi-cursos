import { PaintBluePipe } from './paint-blue.pipe';

describe('PaintBluePipe', () => {
  it('create an instance', () => {
    const pipe = new PaintBluePipe();
    expect(pipe).toBeTruthy();
  });
});
